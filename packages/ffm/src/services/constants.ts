// Enums related to FFM dynamic linking - starts

export enum DynamicLinkTypes {
  PropertyVisitAssignment = 'PROPERTY_VISIT_ASSIGNMENT',
}

export enum DynamicLinkParamKeys {
  Type = 'type',
  RouteType = 'routeType',
  VisitId = 'visitId',
}

// Enums related to dynamic linking - ends
